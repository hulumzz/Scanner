const ImagePreprocessor=(()=>{async function compress(file){if(file.size>20*1024*1024)throw new Error('File melebihi 20 MB');const b=await createImageBitmap(file,{imageOrientation:'from-image'});let scale=Math.min(1,2560/Math.max(b.width,b.height)),w=Math.round(b.width*scale),h=Math.round(b.height*scale);const c=document.createElement('canvas');c.width=w;c.height=h;const x=c.getContext('2d',{alpha:false});x.fillStyle='#fff';x.fillRect(0,0,w,h);x.drawImage(b,0,0,w,h);b.close();let q=.9,blob;do{blob=await new Promise(r=>c.toBlob(r,'image/jpeg',q));q-=.03}while(blob&&blob.size>3*1024*1024&&q>=.8);return{blob,filename:file.name.replace(/\.[^.]+$/,'.jpg'),width:w,height:h}}return{compress}})();